console.log("Ready to go! 🚀");

// Exercise 1
const randomNum = Math.random() * 100;
const roundedValue = Math.round(randomNum);
const toFixedDecimals = Number(randomNum.toFixed(2));

// Exercise 2
const maxValue = Math.max(2, 8, -10, 0, -4, -7, 9);
const minValue = Math.min(2, 8, -10, 0, -4, -7, 9);
